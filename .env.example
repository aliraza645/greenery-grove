# Set this to the running Express API. The frontend services in
# src/services/* call axios with this baseURL and attach the JWT from
# localStorage automatically.
VITE_API_URL=http://localhost:5000/api
